<Window x:Class="FTTHBasemap.UI.ImportSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Selection" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        BorderThickness="0"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E22" />
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#2A2A30" />
        <SolidColorBrush x:Key="AccentBrush" Color="#E74C3C" /> <!-- Red Accent as requested by user -->
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#C0392B" />
        <SolidColorBrush x:Key="BorderBrush" Color="#3E3E44" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#9E9EAE" />
        <SolidColorBrush x:Key="TextWhiteBrush" Color="#E0E0E6" />
        
        <!-- CheckBox Dark Theme -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E6"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>
        <!-- ComboBox Styles for Dark Theme Visibility (Adopted from BasemapPanel.xaml) -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2A2A30" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4,8,4" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition MinWidth="24" Width="0" SharedSizeGroup="ComboBoxButton" />
                            </Grid.ColumnDefinitions>
                            
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="true"
                                   Grid.ColumnSpan="2"
                                   Placement="Bottom"
                                   Margin="1"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade">
                                <Border x:Name="DropDownBorder"
                                        Background="#2A2A30"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        MinWidth="{Binding ActualWidth, ElementName=MainGrid}"
                                        Margin="0,2,0,0">
                                    <ScrollViewer x:Name="DropDownScrollViewer" CanContentScroll="true" VerticalScrollBarVisibility="Auto">
                                        <Grid x:Name="Grid" RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas x:Name="Canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                                <Rectangle x:Name="OpaqueRect"
                                                           Fill="{Binding Background, ElementName=DropDownBorder}"
                                                           Height="{Binding ActualHeight, ElementName=DropDownBorder}"
                                                           Width="{Binding ActualWidth, ElementName=DropDownBorder}" />
                                            </Canvas>
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                            
                            <ToggleButton Grid.ColumnSpan="2"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="6"
                                                SnapsToDevicePixels="true">
                                            <Grid HorizontalAlignment="Right" Width="24">
                                                <Path x:Name="Arrow"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="#E0E0E6"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="templateRoot" Property="Background" Value="#35353C" />
                                                <Setter TargetName="templateRoot" Property="BorderBrush" Value="#007ACC" />
                                                <Setter TargetName="Arrow" Property="Fill" Value="White" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="true">
                                                <Setter TargetName="templateRoot" Property="Background" Value="#1E1E22" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <ContentPresenter IsHitTestVisible="false"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="PART_Popup" Property="IsOpen" Value="false" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#555555" />
                                <Setter Property="Background" Value="#1E1E22" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2A2A30" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="6,4,6,4" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#007ACC" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007ACC" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- Accent Button Style -->
        <Style x:Key="AccentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4" />
                </Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#333339" />
            <Setter Property="Foreground" Value="#E0E0E6" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4" />
                </Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#44444C" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1" 
            CornerRadius="12">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- Content Panel -->
                <RowDefinition Height="Auto" /> <!-- Point Mapping Settings (KML only) -->
                <RowDefinition Height="Auto" /> <!-- Footer Buttons -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="TxtHeaderIcon" Text="📂" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TxtHeaderTitle" Text="IMPORT SELECTION" FontSize="13" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Content="✕" Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Foreground="{StaticResource TextMutedBrush}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnClose_Click"
                        FontSize="11" FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Content Panel (Switches between KML list and PDF layers list) -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                
                <!-- KML Mode Selection Panel -->
                <Grid x:Name="GridKmlMode" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Select features to import from KML/KMZ:" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,4"/>
                    
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="SELECT ALL" FontSize="10" Padding="8,2,8,2" Margin="0,0,8,0" Style="{StaticResource SecondaryButtonStyle}" Click="BtnSelectAllKml_Click"/>
                        <Button Content="DESELECT ALL" FontSize="10" Padding="8,2,8,2" Style="{StaticResource SecondaryButtonStyle}" Click="BtnDeselectAllKml_Click"/>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#26262B" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="6">
                        <TreeView x:Name="TreeKmlFeatures" Background="Transparent" BorderThickness="0" Foreground="White">
                            <!-- Hierarchical templates can be defined in code behind to keep it simple and dynamic -->
                        </TreeView>
                    </Border>
                </Grid>

                <!-- PDF Mode Selection Panel -->
                <Grid x:Name="GridPdfMode" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Warning: Target layer not found. Select the boundary layer manually:" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,8"/>
                    
                    <Border Grid.Row="1" Background="#26262B" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
                        <ListBox x:Name="LstPdfLayers" Background="Transparent" BorderThickness="0" Foreground="White" SelectionMode="Single">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="White" Padding="6,4,6,4"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>
            </Grid>

            <!-- Point Mapping Options (Visible in KML Mode only) -->
            <StackPanel Grid.Row="2" x:Name="PanelPointOptions" Margin="0,0,0,16" Visibility="Visible">
                <Separator Background="{StaticResource BorderBrush}" Margin="0,0,0,10"/>
                <CheckBox x:Name="ChkPlaceBlocks" Content="Place asset blocks on point markers instead of dots" FontWeight="SemiBold" Checked="ChkPlaceBlocks_Changed" Unchecked="ChkPlaceBlocks_Changed"/>
                <Grid x:Name="GridBlockSelection" Margin="20,6,0,0" IsEnabled="False">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Select Block Type:" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" FontSize="11"/>
                    <ComboBox Grid.Column="2" x:Name="CboBlockType" Height="26">
                        <ComboBoxItem Content="NP 7 4&quot; (New Pole 7m 4&quot;)" Tag="NP 7 4&quot;" IsSelected="True"/>
                        <ComboBoxItem Content="NP 7 3&quot; (New Pole 7m 3&quot;)" Tag="NP 7 3&quot;"/>
                        <ComboBoxItem Content="NP 7 2.5&quot; (New Pole 7m 2.5&quot;)" Tag="NP 7 2.5&quot;"/>
                        <ComboBoxItem Content="NP 9 4&quot; (New Pole 9m 4&quot;)" Tag="NP 9 4&quot;"/>
                        <ComboBoxItem Content="EXT TEL (Existing Telkom Pole)" Tag="EXT TEL"/>
                        <ComboBoxItem Content="EXT 7 4&quot; (Existing Pole 7m 4&quot;)" Tag="EXT 7 4&quot;"/>
                        <ComboBoxItem Content="EXT 7 3&quot; (Existing Pole 7m 3&quot;)" Tag="EXT 7 3&quot;"/>
                        <ComboBoxItem Content="EXT 7 2.5&quot; (Existing Pole 7m 2.5&quot;)" Tag="EXT 7 2.5&quot;"/>
                        <ComboBoxItem Content="EXT 9 4&quot; (Existing Pole 9m 4&quot;)" Tag="EXT 9 4&quot;"/>
                        <ComboBoxItem Content="FAT Label (FAT Table Block)" Tag="FAT Label.JSON"/>
                        <ComboBoxItem Content="FDT 48C (FDT Block 48C)" Tag="FDT48.JSON"/>
                        <ComboBoxItem Content="FDT 72C (FDT Block 72C)" Tag="FDT72.JSON"/>
                    </ComboBox>
                </Grid>
            </StackPanel>

            <!-- Footer Buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="CANCEL" Height="32" Style="{StaticResource SecondaryButtonStyle}" Click="BtnClose_Click" />
                <Button Grid.Column="2" Content="IMPORT SELECTED" Height="32" Style="{StaticResource AccentButtonStyle}" Click="BtnImport_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
