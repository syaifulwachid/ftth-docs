<Window x:Class="FTTHBasemap.UI.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Developer" Height="250" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        BorderThickness="0"
        MouseDown="Window_MouseDown">
    
    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E22" />
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#2A2A30" />
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#1C97EA" />
        <SolidColorBrush x:Key="BorderBrush" Color="#3E3E44" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#9E9EAE" />
        <SolidColorBrush x:Key="TextWhiteBrush" Color="#E0E0E6" />
    </Window.Resources>
    
    <Border Background="{StaticResource BackgroundBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1" 
            CornerRadius="10">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- Content -->
                <RowDefinition Height="Auto" /> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="DEVELOPER INFO" FontSize="13" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                <Button Content="✕" Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Foreground="{StaticResource TextMutedBrush}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnClose_Click"
                        FontSize="11" FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Content Card -->
            <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Avatar Initials -->
                    <Grid Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                        <Ellipse Width="44" Height="44" Fill="#15007ACC" Stroke="{StaticResource AccentBrush}" StrokeThickness="1"/>
                        <TextBlock Text="SW" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource AccentBrush}" FontWeight="Bold" FontSize="15"/>
                    </Grid>

                    <!-- Info -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Dibuat oleh: " Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                            <TextBlock Text="Syaiful Wachid" Foreground="{StaticResource TextWhiteBrush}" FontSize="11" FontWeight="Bold" />
                            <TextBlock Text=" (SWD Developer)" Foreground="{StaticResource TextMutedBrush}" FontSize="10" FontStyle="Italic" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Senior Project Designer: " Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                            <TextBlock Text="Fiberhome Indonesia" Foreground="{StaticResource TextWhiteBrush}" FontSize="11" FontWeight="SemiBold" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Linkedin Profile: " Foreground="{StaticResource TextMutedBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="Syaiful Wachid" 
                                       Foreground="#1C97EA" 
                                       TextDecorations="Underline" 
                                       Cursor="Hand" 
                                       FontSize="11"
                                       VerticalAlignment="Center"
                                       PreviewMouseDown="TxtLinkedin_MouseDown"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Close Button -->
            <Button Grid.Row="2" Content="Close" Width="70" Height="26" Margin="0,12,0,0" HorizontalAlignment="Right" Click="BtnClose_Click" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#333339" />
                        <Setter Property="Foreground" Value="#E0E0E6" />
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#44444C" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
